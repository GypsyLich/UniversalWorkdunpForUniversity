#pragma once

int febo(int previous, int current, int counter, int targetNumber);
int febo_iterative(int n);
int sum_array(int *array, int arrSize, int container);
int sum_array_iterative(int *array, int arrSize);